/node_modules
/.next/
/out/
/build
*.pem
.env*.local
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.tsbuildinfo
next-env.d.ts

.vercel
.vapid.json
.claude/
build.log
.agents/

# Local working documents — reports and notes that live beside the code but
# are not part of it. Keeps `git add -A` from sweeping binaries into the repo.
*.docx
~$*
